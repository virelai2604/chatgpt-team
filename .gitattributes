# ============================================================
# BIFL Line Endings Policy (Linux-first: WSL + Render)
# Goal:
#   - Normalize text to LF in the repository
#   - Enforce LF in working trees for key text formats (esp. CSV)
# ============================================================

# Auto-detect text vs binary and normalize text to LF in repo.
* text=auto

# --- Source code ---
*.py     text eol=lf
*.pyi    text eol=lf
*.sh     text eol=lf
*.bash   text eol=lf
*.zsh    text eol=lf

# --- Config / data / docs ---
*.md     text eol=lf
*.txt    text eol=lf
*.json   text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.toml   text eol=lf
*.ini    text eol=lf
*.env    text eol=lf
*.csv    text eol=lf

# --- Web / misc text ---
*.html   text eol=lf
*.css    text eol=lf
*.js     text eol=lf
*.ts     text eol=lf
*.tsx    text eol=lf

# --- Binaries (never normalize) ---
*.png    -text
*.jpg    -text
*.jpeg   -text
*.gif    -text
*.webp   -text
*.ico    -text
*.pdf    -text
*.zip    -text
*.gz     -text
*.7z     -text
*.woff   -text
*.woff2  -text
*.ttf    -text
*.otf    -text
