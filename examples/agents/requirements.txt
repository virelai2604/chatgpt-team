# Client-side deps for the agents example. Kept separate from the relay's own
# runtime requirements — agents are consumers of the relay, not part of it.
openai-agents>=0.21,<0.22

# Same openai major as the rest of the repo. This example was previously held back
# on the 2.x line because every published openai-agents required `openai<3` --
# "feat: support OpenAI Python 3 and HTTPX2" (#4380) had landed on upstream `main`
# but was unreleased. 0.21.0 ships it and declares `openai>=3.0.0,<4`, so the pins
# move together here and the example no longer needs its own virtualenv.
openai>=3.0,<4.0

# Only needed for agent_server.py (exposing the agent over HTTP).
fastapi>=0.138,<1.0
uvicorn[standard]>=0.49,<1.0
