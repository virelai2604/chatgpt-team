---
source_id: gh_openai_developers_for_cursor
title: openai/openai-developers-for-cursor — Cursor-side sibling of the OpenAI developer bundle
category: github_reference
source_urls:
  - https://github.com/openai/openai-developers-for-cursor
retrieved_at: 2026-08-15
pinned_commit: 9120f1f6bb47964651ab822e24b4f358e3638ae3
fetch_method: shallow clone (--depth 1 --filter=blob:none), read locally
pull_status: fetched
verify: check whether its rules/skills have diverged from the Claude bundle before assuming parity
---

# openai/openai-developers-for-cursor

> Provenance: `fetched` 2026-08-15 at `9120f1f`. Reference only — not vendored.

Cursor-side counterpart of `openai-developers-for-claude` (see that file — it carries the
detail). Both are small (~380 KB) and both HEAD at a "Sync latest-model routing" commit,
which suggests they are generated from a shared source and kept in step.

## Relevance

Only if Cursor is part of the workflow. If Claude Code is the editor, the Claude bundle is
the one that matters and this is here for completeness — recorded so a future reader does
not have to re-establish that the two are siblings rather than alternatives with different
content.
