# Runtime dependencies only. This is what Render installs (see render.yaml:
# buildCommand -> `pip install -r requirements.txt`), so it must NOT pull the
# dev extra — pytest and friends have no business in the deployed service.
#
# For a development or CI environment, use requirements-dev.txt instead.
-e .
