{
  "schema_version": "v1.2",
  "updated_at": "2025-11-02T22:45:00Z",
  "description": "Manifest of all enabled tools for ChatGPT Team Relay. Each tool is declared with its entrypoint and configuration for execution via /v1/tools/* routes or /v1/responses tool calls.",
  "tools": [
    {
      "tool_name": "multiply",
      "enabled": true,
      "version": "1.0",
      "description": "Multiply two numbers and return the result.",
      "entrypoint": "app.tools.math_tools.multiply",
      "parameters": {
        "type": "object",
        "properties": {
          "a": { "type": "number", "description": "First number" },
          "b": { "type": "number", "description": "Second number" }
        },
        "required": ["a", "b"]
      }
    },
    {
      "tool_name": "file_search",
      "enabled": true,
      "version": "1.0",
      "description": "Search uploaded files by keyword using vector similarity.",
      "entrypoint": "app.api.tools.file_search.run",
      "parameters": {
        "type": "object",
        "properties": {
          "query": { "type": "string", "description": "Search query text" },
          "limit": { "type": "integer", "default": 5 }
        },
        "required": ["query"]
      }
    },
    {
      "tool_name": "image_generation",
      "enabled": true,
      "version": "1.0",
      "description": "Generate an image from text using DALL·E-compatible generation.",
      "entrypoint": "app.api.tools.image_generation.create",
      "parameters": {
        "type": "object",
        "properties": {
          "prompt": { "type": "string" },
          "size": { "type": "string", "enum": ["256x256", "512x512", "1024x1024"], "default": "512x512" }
        },
        "required": ["prompt"]
      }
    },
    {
      "tool_name": "web_search",
      "enabled": true,
      "version": "1.0",
      "description": "Perform a live web search via relay web_search tool.",
      "entrypoint": "app.api.tools.web_search.query",
      "parameters": {
        "type": "object",
        "properties": {
          "query": { "type": "string", "description": "Text query" },
          "region": { "type": "string", "description": "Region bias", "default": "us" }
        },
        "required": ["query"]
      }
    },
    {
      "tool_name": "vector_store_retrieval",
      "enabled": true,
      "version": "1.0",
      "description": "Retrieve content from a vector store by similarity search.",
      "entrypoint": "app.api.tools.vector_store_retrieval.retrieve",
      "parameters": {
        "type": "object",
        "properties": {
          "query": { "type": "string" },
          "top_k": { "type": "integer", "default": 3 }
        },
        "required": ["query"]
      }
    },
    {
      "tool_name": "code_interpreter",
      "enabled": true,
      "version": "1.0",
      "description": "Run Python snippets in a sandboxed environment.",
      "entrypoint": "app.api.tools.code_interpreter.execute",
      "parameters": {
        "type": "object",
        "properties": {
          "code": { "type": "string", "description": "Python code to execute" }
        },
        "required": ["code"]
      }
    }
  ]
}
