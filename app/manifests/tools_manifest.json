{
  "object": "list",
  "data": [
    {
      "id": "web_search",
      "name": "web_search",
      "object": "tool",
      "type": "function",
      "description": "Search the web for up-to-date information, news, and facts.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query string describing what to look up."
          },
          "recency_days": {
            "type": "integer",
            "description": "How many days back to prioritize in the search (1 = last 24h, 7 = last week, etc.).",
            "minimum": 1,
            "default": 7
          },
          "max_results": {
            "type": "integer",
            "description": "Maximum number of search results to retrieve.",
            "minimum": 1,
            "maximum": 20,
            "default": 5
          }
        },
        "required": ["query"]
      }
    },
    {
      "id": "file_search",
      "name": "file_search",
      "object": "tool",
      "type": "function",
      "description": "Search across files and vector stores connected to this relay.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query string describing what to look up in files or vector stores."
          },
          "top_k": {
            "type": "integer",
            "description": "Maximum number of relevant chunks or documents to return.",
            "minimum": 1,
            "maximum": 50,
            "default": 10
          },
          "filters": {
            "type": "object",
            "description": "Optional metadata filters to narrow search results (e.g. file type, tags, or source).",
            "additionalProperties": {
              "type": ["string", "number", "boolean"]
            }
          }
        },
        "required": ["query"]
      }
    },
    {
      "id": "code_interpreter",
      "name": "code_interpreter",
      "object": "tool",
      "type": "function",
      "description": "Execute Python code in a sandbox for calculations, data analysis, and other utilities.",
      "parameters": {
        "type": "object",
        "properties": {
          "code": {
            "type": "string",
            "description": "Python code to execute."
          },
          "files": {
            "type": "array",
            "description": "Optional list of file IDs to make available in the runtime environment.",
            "items": {
              "type": "string"
            }
          }
        },
        "required": ["code"]
      }
    },
    {
      "id": "image_generation",
      "name": "image_generation",
      "object": "tool",
      "type": "function",
      "description": "Generate images using the OpenAI image generation API via this relay.",
      "parameters": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "Text prompt describing the desired image."
          },
          "size": {
            "type": "string",
            "description": "Image size, e.g. '1024x1024', '512x512'.",
            "default": "1024x1024"
          },
          "n": {
            "type": "integer",
            "description": "Number of images to generate.",
            "minimum": 1,
            "maximum": 10,
            "default": 1
          },
          "response_format": {
            "type": "string",
            "description": "Response format for the generated image(s), e.g. 'b64_json' or 'url'.",
            "default": "b64_json"
          }
        },
        "required": ["prompt"]
      }
    },
    {
      "id": "audio_transcription",
      "name": "audio_transcription",
      "object": "tool",
      "type": "function",
      "description": "Transcribe audio files into text using OpenAI audio models.",
      "parameters": {
        "type": "object",
        "properties": {
          "file_id": {
            "type": "string",
            "description": "File ID of the audio to transcribe."
          },
          "language": {
            "type": "string",
            "description": "Optional language code for the transcription (e.g. 'en')."
          },
          "prompt": {
            "type": "string",
            "description": "Optional prompt to guide the transcription style or context."
          }
        },
        "required": ["file_id"]
      }
    },
    {
      "id": "audio_speech",
      "name": "audio_speech",
      "object": "tool",
      "type": "function",
      "description": "Convert text to speech audio using OpenAI text-to-speech models.",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "string",
            "description": "Text to synthesize into speech."
          },
          "voice": {
            "type": "string",
            "description": "Voice preset to use (e.g. 'alloy').",
            "default": "alloy"
          },
          "format": {
            "type": "string",
            "description": "Audio format to return, e.g. 'mp3' or 'wav'.",
            "default": "mp3"
          }
        },
        "required": ["input"]
      }
    },
    {
      "id": "audio_translation",
      "name": "audio_translation",
      "object": "tool",
      "type": "function",
      "description": "Translate spoken audio into another language.",
      "parameters": {
        "type": "object",
        "properties": {
          "file_id": {
            "type": "string",
            "description": "File ID of the audio to translate."
          },
          "target_language": {
            "type": "string",
            "description": "Target language code (e.g. 'en', 'es', 'fr').",
            "default": "en"
          }
        },
        "required": ["file_id"]
      }
    },
    {
      "id": "video_generation",
      "name": "video_generation",
      "object": "tool",
      "type": "function",
      "description": "Generate or edit videos using the OpenAI Video API via this relay.",
      "parameters": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "Text description of the desired video."
          },
          "duration": {
            "type": "number",
            "description": "Approximate video duration in seconds.",
            "default": 5
          },
          "aspect_ratio": {
            "type": "string",
            "description": "Optional aspect ratio, e.g. '16:9', '9:16', or '1:1'.",
            "default": "16:9"
          }
        },
        "required": ["prompt"]
      }
    }
  ]
}
