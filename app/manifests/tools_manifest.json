{
  "object": "list",
  "data": [
    {
      "id": "web_search",
      "name": "web_search",
      "object": "tool",
      "type": "function",
      "description": "Search the web for up-to-date information, news, and facts.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query string describing what to look up."
          },
          "recency_days": {
            "type": "integer",
            "description": "Optional recency filter in days for prioritizing recent results.",
            "minimum": 0
          }
        },
        "required": ["query"]
      }
    },
    {
      "id": "file_search",
      "name": "file_search",
      "object": "tool",
      "type": "function",
      "description": "Search across files and vector stores connected to this relay.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query string."
          },
          "vector_store_ids": {
            "type": "array",
            "description": "Optional list of vector store IDs to scope the search.",
            "items": {
              "type": "string"
            }
          }
        },
        "required": ["query"]
      }
    },
    {
      "id": "code_interpreter",
      "name": "code_interpreter",
      "object": "tool",
      "type": "function",
      "description": "Run small code snippets and perform calculations or transformations in a sandboxed environment.",
      "parameters": {
        "type": "object",
        "properties": {
          "language": {
            "type": "string",
            "description": "Programming language (e.g. 'python').",
            "default": "python"
          },
          "task": {
            "type": "string",
            "description": "Description of the computation or transformation."
          }
        },
        "required": ["task"]
      }
    },
    {
      "id": "image_generation",
      "name": "image_generation",
      "object": "tool",
      "type": "function",
      "description": "Generate images from natural language prompts using the upstream OpenAI images API.",
      "parameters": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "Text prompt describing the desired image."
          },
          "size": {
            "type": "string",
            "description": "Requested image size (e.g. '1024x1024').",
            "default": "1024x1024"
          }
        },
        "required": ["prompt"]
      }
    },
    {
      "id": "audio_transcription",
      "name": "audio_transcription",
      "object": "tool",
      "type": "function",
      "description": "Transcribe audio content to text using upstream OpenAI audio models.",
      "parameters": {
        "type": "object",
        "properties": {
          "file_id": {
            "type": "string",
            "description": "ID of the audio file previously uploaded via /v1/files."
          },
          "language": {
            "type": "string",
            "description": "Language code hint for the transcription (e.g. 'en')."
          }
        },
        "required": ["file_id"]
      }
    },
    {
      "id": "audio_generation",
      "name": "audio_generation",
      "object": "tool",
      "type": "function",
      "description": "Generate spoken audio from text using upstream OpenAI audio generation models.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "The text to read aloud."
          },
          "voice": {
            "type": "string",
            "description": "Optional voice preset to use."
          }
        },
        "required": ["text"]
      }
    },
    {
      "id": "vector_store_search",
      "name": "vector_store_search",
      "object": "tool",
      "type": "function",
      "description": "Search a specific vector store and return relevant file chunks or references.",
      "parameters": {
        "type": "object",
        "properties": {
          "vector_store_id": {
            "type": "string",
            "description": "The ID of the vector store to search."
          },
          "query": {
            "type": "string",
            "description": "Semantic query text."
          },
          "k": {
            "type": "integer",
            "description": "Maximum number of results to return.",
            "default": 8,
            "minimum": 1
          }
        },
        "required": ["vector_store_id", "query"]
      }
    },
    {
      "id": "computer_use",
      "name": "computer_use",
      "object": "tool",
      "type": "function",
      "description": "Placeholder for computer-use style tools (screen interaction, automation, etc.)",
      "parameters": {
        "type": "object",
        "properties": {
          "task": {
            "type": "string",
            "description": "Description of the computer interaction task to perform."
          }
        },
        "required": ["task"]
      }
    }
  ]
}
