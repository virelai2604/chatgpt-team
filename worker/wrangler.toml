name = "chatgpt-team-realtime"
main = "_worker.ts"
compatibility_date = "2025-09-14"  # keep current and valid
workers_dev = true

[vars]
# plain (non-secret) vars are OK in TOML
OPENAI_BETA = "realtime=v1"
UPSTREAM_REALTIME = "1"